# Supabase Database Configuration
# Copy this file to .env.local and fill in your actual values

# Option 1: Use Supabase connection string (recommended)
DATABASE_URL=postgres://hanti_app:YOUR_APP_PASSWORD@YOUR_HOSTNAME:5432/postgres?sslmode=require

# Option 2: Use individual environment variables
DB_HOST=YOUR_HOSTNAME
DB_PORT=5432
DB_NAME=postgres
DB_USER=hanti_app
DB_PASSWORD=YOUR_APP_PASSWORD
DB_SSL=true

# Instructions:
# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings → Database
# 3. Copy the connection string and replace YOUR_HOSTNAME and YOUR_APP_PASSWORD
# 4. Save this file as .env.local (it will be ignored by git)
